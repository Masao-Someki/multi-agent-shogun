{
  "area": "egs3-recipes",
  "coverage": {
    "files_fully_read": [
      "egs3/TEMPLATE/asr/run.py",
      "egs3/TEMPLATE/asr/src/inference.py",
      "egs3/TEMPLATE/asr/src/app.py",
      "egs3/TEMPLATE/asr/readme.md",
      "egs3/TEMPLATE/asr/conf/training.yaml",
      "egs3/TEMPLATE/asr/conf/inference.yaml",
      "egs3/TEMPLATE/asr/conf/metrics.yaml",
      "egs3/TEMPLATE/asr/conf/publication.yaml",
      "egs3/TEMPLATE/asr/conf/demo.yaml",
      "egs3/TEMPLATE/tts/conf/training.yaml",
      "egs3/TEMPLATE/tts/conf/inference.yaml",
      "egs3/TEMPLATE/tts/conf/metrics.yaml",
      "egs3/mini_an4/asr/run.py",
      "egs3/mini_an4/asr/dataset/__init__.py",
      "egs3/mini_an4/asr/dataset/builder.py",
      "egs3/mini_an4/asr/dataset/dataset.py",
      "egs3/mini_an4/asr/dataset/config.yaml",
      "egs3/mini_an4/asr/src/preprocessor.py",
      "egs3/mini_an4/asr/src/tokenizer.py",
      "egs3/mini_an4/asr/src/inference.py",
      "egs3/mini_an4/asr/src/app.py",
      "egs3/mini_an4/asr/path.sh",
      "egs3/mini_an4/asr/readme.md",
      "egs3/mini_an4/asr/conf/demo.yaml",
      "egs3/mini_an4/asr/conf/inference.yaml",
      "egs3/mini_an4/asr/conf/inference_transducer.yaml",
      "egs3/mini_an4/asr/conf/metrics.yaml",
      "egs3/mini_an4/asr/conf/publication.yaml",
      "egs3/mini_an4/asr/conf/training.yaml (symlink -> training_transducer_asr_conformer_rnnt.yaml)",
      "egs3/mini_an4/asr/conf/training_asr_streaming.yaml",
      "egs3/mini_an4/asr/conf/training_asr_transducer.yaml",
      "egs3/mini_an4/asr/conf/training_asr_transformer.yaml",
      "egs3/mini_an4/asr/conf/training_asr_transformer_split_preprocessor.yaml",
      "egs3/mini_an4/asr/conf/training_transducer_asr_conformer_rnnt.yaml",
      "egs3/librispeech_100/asr/run.py",
      "egs3/librispeech_100/asr/dataset/__init__.py",
      "egs3/librispeech_100/asr/dataset/builder.py",
      "egs3/librispeech_100/asr/dataset/dataset.py",
      "egs3/librispeech_100/asr/dataset/config.yaml",
      "egs3/librispeech_100/asr/src/tokenizer.py",
      "egs3/librispeech_100/asr/src/inference.py",
      "egs3/librispeech_100/asr/src/app.py",
      "egs3/librispeech_100/asr/path.sh",
      "egs3/librispeech_100/asr/readme.md",
      "egs3/librispeech_100/asr/conf/demo.yaml",
      "egs3/librispeech_100/asr/conf/inference.yaml",
      "egs3/librispeech_100/asr/conf/publication.yaml",
      "egs3/librispeech_100/asr/conf/metrics.yaml",
      "egs3/librispeech_100/asr/conf/tuning/training_e_branchformer.yaml",
      "ci/test_integration_espnet3.sh"
    ],
    "files_skimmed": [
      "espnet3/utils/config_utils.py (load_and_merge_config/load_config_with_defaults, to understand merge semantics)",
      "espnet3/systems/base/system.py (create_dataset/collect_stats/train/infer/measure stage stubs)",
      "espnet3/components/data/dataset_builder.py (DatasetBuilder ABC contract)",
      "espnet2/bin/asr_inference.py (Speech2Text.__call__ return shape, transducer-decoder handling)",
      "espnet2/bin/asr_transducer_inference.py (Speech2Text.__call__ / hypotheses_to_results return shape)",
      "espnet3/utils/stages_utils.py (rank handling around create_dataset stage)"
    ],
    "files_not_read": [],
    "tests_inspected": [],
    "notes": "test_file_assessments left empty per instructions (not a test-quality reviewer). Verified two claims by execution from SCRATCH: (1) load_and_merge_config on mini_an4's real training.yaml merged against egs3.TEMPLATE.asr shows create_dataset={'func': 'src.creating_dataset.create_dataset', 'dataset_dir': '/path/to/your/dataset', 'recipe_dir': '.'}, confirming the dead 'func' hook and placeholder dataset_dir leak into the real recipe's resolved config. (2) grep confirmed build_output_transducer (TEMPLATE/asr/src/inference.py) is referenced by no config/test anywhere in the tree -- dead code. Checked but found OK: TEMPLATE's readme step 3/4 infer+measure without --training_config are just instances of the already-known 'measure crashes on missing inference_dir' family, not filed separately. mini_an4's dataset/preprocessor/tokenizer/inference contracts (build_output indexing, split-based instantiation) were cross-checked against espnet2 Speech2Text/asr_transducer_inference return shapes and are internally consistent. Did not execute a live 'kill mid-download' repro for the prepare_source() non-atomicity finding (would require network access / real archive; assessed via careful code reading of prepare_source()/is_source_prepared() instead). Did not deep-dive espnet3/utils/run_utils.py (apply_training_experiment_context/validate_experiment_context) or espnet3/components/data/data_organizer.py since they are outside this area's assigned file list; findings that depend on their exact behavior (publication.yaml exp_dir convention drift, create_dataset stage rank-guarding) are flagged with reduced confidence accordingly."
  },
  "assumptions": [
    "load_and_merge_config(config_path=None, ...) returns None (confirmed by reading espnet3/utils/config_utils.py:394-396), so run.py's per-stage 'required config' guardrail is only bypassed when a config IS supplied but is missing a field the stage needs downstream (matches the already-known measure/inference_dir bug family).",
    "BaseSystem.create_dataset() (espnet3/systems/base/system.py) is the actual implementation used by ASRSystem for the create_dataset stage (ASRSystem does not override it, confirmed by grep).",
    "DatasetBuilder subclasses are expected to accept arbitrary **kwargs per the ABC's documented contract, so extra keys forwarded from create_dataset_config (func, dataset_dir) do not crash mini_an4/librispeech's builders, but do so silently rather than by design of those particular keys."
  ],
  "test_file_assessments": [],
  "findings": [
    {
      "file": "egs3/mini_an4/asr/dataset/builder.py",
      "lines": "92-134",
      "category": "bug",
      "severity": "high",
      "summary": "MiniAn4Builder.prepare_source() downloads the archive directly to its final path with no atomicity or completeness marker, so an interrupted download (Ctrl-C, OOM-kill, preemption, network drop) leaves a truncated downloads.tar.gz that is_source_prepared()/prepare_source() treat as already-present on the next run, and is_source_prepared() only checks directory existence afterward so a partial extraction can be reported as fully prepared forever.",
      "scenario": "First `python run.py --stages create_dataset` is killed while urlretrieve(url, archive) (line 125) is mid-write. downloads.tar.gz now exists but is truncated. Re-running create_dataset: `archive.exists()` (line 120) is True, so the download is skipped; `tar -xzf` (line 130-133) either raises CalledProcessError with an unhelpful generic tar error (no message pointing at the real cause), or partially extracts an4/etc and an4/wav with some files missing before erroring. On any later invocation, `is_source_prepared()` (lines 64-90) reports True as soon as both directories exist at all -- it never re-validates completeness -- so `build()` (lines 192-242) proceeds against an incomplete source tree, either crashing deep inside the transcript loop when a referenced .sph file is missing, or (combined with the already-known sph2pipe zero-byte-wav bug) silently writing manifest rows that point at empty wav files.",
      "evidence": "prepare_source(): `if not archive.exists(): ... urlretrieve(url, str(archive))` (lines 120-125) writes straight to the final path with no temp file/rename/checksum. is_source_prepared(): `return (an4/etc).is_dir() and (an4/wav).is_dir()` (line 90) is existence-only, no size/manifest check.",
      "suggested_fix": "Download to a temp path in the same directory (e.g. archive.with_suffix('.tmp')) and os.replace() it into place only after the download completes successfully; optionally verify the extracted tree against a known file count or checksum before returning True from is_source_prepared().",
      "confidence": 0.7,
      "test_name": null,
      "related_files": ["espnet3/components/data/dataset_builder.py", "espnet3/systems/base/system.py"],
      "verified_by_execution": false
    },
    {
      "file": "egs3/TEMPLATE/asr/conf/training.yaml",
      "lines": "46-53",
      "category": "docs-mismatch",
      "severity": "high",
      "summary": "The DATASET CREATION SETTING block documents a `create_dataset.func` hook ('The function is resolved from create_dataset.func and called with the remaining fields in this block as keyword arguments') that the actual create_dataset stage never reads -- BaseSystem.create_dataset() ignores `func`/`dataset_dir` entirely and instead loads the recipe's dataset/__init__.py DatasetBuilder class.",
      "scenario": "A new recipe author follows the canonical TEMPLATE, writes `src/creating_dataset.py` with a `create_dataset(dataset_dir=...)` function matching the documented contract, and expects run.py --stages create_dataset to call it. It never runs: BaseSystem.create_dataset() (espnet3/systems/base/system.py:226-278) does `default_builder_kwargs = dict(create_dataset_config)` and then, per dataset entry, loads `module.DatasetBuilder()` from the recipe's dataset/__init__.py and calls is_source_prepared/prepare_source/is_built/build(**builder_kwargs) -- `func` is just an inert extra kwarg silently absorbed by the builder's **kwargs. Neither mini_an4 nor librispeech_100 (the two real recipes) ship a src/creating_dataset.py, consistent with the hook being dead everywhere in the tree.",
      "evidence": "Verified by execution: loading mini_an4's real conf/training.yaml through load_and_merge_config(..., default_package='egs3.TEMPLATE.asr') yields `cfg.create_dataset == {'func': 'src.creating_dataset.create_dataset', 'dataset_dir': '/path/to/your/dataset', 'recipe_dir': '.'}` -- the TEMPLATE placeholder leaks straight into the real recipe's resolved config, unused. grep -rn 'create_dataset.func' espnet3/ finds no reader; espnet3/systems/base/system.py:239 `default_builder_kwargs = dict(create_dataset_config)` is the only consumer and it never looks at the 'func' key.",
      "suggested_fix": "Either implement the documented func-hook mechanism in BaseSystem.create_dataset(), or delete the func/dataset_dir fields and the misleading comment from TEMPLATE/asr/conf/training.yaml and document the actual dataset/__init__.py:DatasetBuilder contract instead.",
      "confidence": 0.9,
      "test_name": null,
      "related_files": ["espnet3/systems/base/system.py", "espnet3/components/data/dataset_builder.py", "egs3/mini_an4/asr/conf/training.yaml", "egs3/librispeech_100/asr/conf/tuning/training_e_branchformer.yaml"],
      "verified_by_execution": true
    },
    {
      "file": "egs3/TEMPLATE/asr/readme.md",
      "lines": "13-14",
      "category": "docs-mismatch",
      "severity": "medium",
      "summary": "TEMPLATE/asr's readme -- the generic canonical-runner template -- opens its Quick start with a LibriSpeech-specific, factually wrong step ('Convert LibriSpeech to Hugging Face format (run once)') that matches neither TEMPLATE's own generic create_dataset mechanism nor either real recipe's actual behavior.",
      "scenario": "A user copies egs3/TEMPLATE/asr as a starting point for an unrelated dataset, follows readme.md step 1 expecting a Hugging Face conversion step, but TEMPLATE's create_dataset stage does not convert anything to 'Hugging Face format' (see the create_dataset.func dead-hook finding above) and no shipped recipe does either: mini_an4 builds TSV manifests from AN4 (egs3/mini_an4/asr/dataset/builder.py), and librispeech_100's builder only validates that raw LibriSpeech directories are already present on disk (egs3/librispeech_100/asr/dataset/builder.py:85-153, prepare_source() just raises FileNotFoundError if splits are missing -- no conversion of any kind). librispeech_100/asr/readme.md itself says 'Place the corpus under download/LibriSpeech ... before running create_dataset/train' and its own Quick start skips create_dataset entirely, confirming TEMPLATE's text is stale leftover copy, not a generalized instruction.",
      "evidence": "TEMPLATE/asr/readme.md:13-14: `# 1) Convert LibriSpeech to Hugging Face format (run once)\\npython run.py --stages create_dataset --training_config conf/training.yaml`. Contrast with librispeech_100/asr/readme.md:1-5 ('Place the corpus under download/LibriSpeech...') and egs3/librispeech_100/asr/dataset/builder.py:108-132 (prepare_source only validates, never converts).",
      "suggested_fix": "Replace the LibriSpeech-specific comment in TEMPLATE/asr/readme.md with a generic placeholder describing what create_dataset does for *this* template (build/validate the recipe's dataset via dataset/__init__.py:DatasetBuilder), matching the DATASET CREATION SETTING comments already in TEMPLATE/asr/conf/training.yaml.",
      "confidence": 0.75,
      "test_name": null,
      "related_files": ["egs3/librispeech_100/asr/readme.md", "egs3/librispeech_100/asr/dataset/builder.py", "egs3/mini_an4/asr/dataset/builder.py"],
      "verified_by_execution": false
    },
    {
      "file": "egs3/TEMPLATE/asr/conf/publication.yaml",
      "lines": "27",
      "category": "design",
      "severity": "low",
      "summary": "publication.yaml's exp_dir omits the ${recipe_dir} prefix that training.yaml and inference.yaml both use, so a recipe with a non-default recipe_dir gets a different exp_dir for standalone pack_model/upload_model than for train/infer.",
      "scenario": "A recipe sets a non-default `recipe_dir` (e.g. runs run.py from a different cwd with recipe_dir overridden) and runs `pack_model`/`upload_model` standalone (only --publication_config, no --training_config, which the publication.yaml comments explicitly say is supported: 'Override inference_dir only when...'). training.yaml/inference.yaml resolve exp_dir as ${recipe_dir}/exp/${exp_tag}; publication.yaml resolves it as plain exp/${exp_tag} relative to CWD instead of recipe_dir, so pack_model looks for the experiment in the wrong directory when CWD != recipe_dir.",
      "evidence": "egs3/TEMPLATE/asr/conf/training.yaml:28 `exp_dir: ${recipe_dir}/exp/${exp_tag}`; egs3/TEMPLATE/asr/conf/inference.yaml:23 same; egs3/TEMPLATE/asr/conf/publication.yaml:27 `exp_dir: exp/${exp_tag}` (no ${recipe_dir}).",
      "suggested_fix": "Change publication.yaml line 27 to `exp_dir: ${recipe_dir}/exp/${exp_tag}` for consistency with training.yaml/inference.yaml.",
      "confidence": 0.5,
      "test_name": null,
      "related_files": ["espnet3/utils/run_utils.py"],
      "verified_by_execution": false
    },
    {
      "file": "egs3/TEMPLATE/asr/src/app.py",
      "lines": "1-111",
      "category": "design",
      "severity": "low",
      "summary": "The ~110-line Gradio launcher (build_demo/main) is duplicated verbatim across TEMPLATE/asr/src/app.py, mini_an4/asr/src/app.py, and librispeech_100/asr/src/app.py instead of living once as a shared helper that recipe-local app.py files import.",
      "scenario": "Any future change to the positional input/output component wiring, error handling, or logging in the demo launcher (e.g. fixing a Gradio API change) must be manually replicated in every recipe's src/app.py; missing one leaves that recipe's packed demo silently behind or broken relative to the others.",
      "evidence": "`diff egs3/mini_an4/asr/src/app.py egs3/librispeech_100/asr/src/app.py` and `diff egs3/TEMPLATE/asr/src/app.py egs3/librispeech_100/asr/src/app.py` both report no differences (byte-identical files).",
      "suggested_fix": "Move build_demo()/main() into espnet3/publication/demo/ (e.g. a launch_gradio_app helper) and have each recipe's src/app.py be a thin wrapper that imports and calls it, keeping only the `if __name__ == '__main__'` entry point recipe-local if needed at all.",
      "confidence": 0.6,
      "test_name": null,
      "related_files": ["egs3/mini_an4/asr/src/app.py", "egs3/librispeech_100/asr/src/app.py", "espnet3/publication/demo/session.py"],
      "verified_by_execution": false
    },
    {
      "file": "egs3/TEMPLATE/asr/src/inference.py",
      "lines": "12-17",
      "category": "robustness",
      "severity": "low",
      "summary": "build_output_transducer is dead code: it is not referenced by output_fn in any shipped config (mini_an4's transducer config uses src.inference.build_output instead) or by any test, so it is unexercised and can silently rot / mislead a recipe author into thinking it is the sanctioned transducer output formatter.",
      "scenario": "A recipe author sees build_output_transducer in TEMPLATE/asr/src/inference.py, assumes it is the correct output_fn to pair with espnet2.bin.asr_transducer_inference.Speech2Text, and sets `output_fn: src.inference.build_output_transducer`. If return_decoded_hyp is left at its default False, Speech2Text.__call__ returns raw Hypothesis objects (not (text, token, token_int, hyp) tuples), so `hyp = model_output[0]` (line 15) yields a Hypothesis object rather than a string, which then gets written into the hyp SCP file as a garbage repr instead of decoded text.",
      "evidence": "grep -rn 'build_output_transducer' egs3/ espnet3/ test/ finds only the definition at egs3/TEMPLATE/asr/src/inference.py:12, no callers. mini_an4/asr/conf/inference_transducer.yaml:56 instead sets `output_fn: src.inference.build_output` together with `return_decoded_hyp: true` (line 53), which is the combination that actually produces text via espnet2/bin/asr_transducer_inference.py's hypotheses_to_results() (returns (text, token, token_int, hyp) tuples, matching build_output's `model_output[0][0]` indexing).",
      "suggested_fix": "Either delete build_output_transducer if it is truly unneeded, or wire it into mini_an4's inference_transducer.yaml with a comment clarifying it is only correct when return_decoded_hyp is left False, since as written the two output_fn variants in this file are easy to mismatch with return_decoded_hyp.",
      "confidence": 0.55,
      "test_name": null,
      "related_files": ["egs3/mini_an4/asr/conf/inference_transducer.yaml", "espnet2/bin/asr_transducer_inference.py"],
      "verified_by_execution": false
    }
  ]
}
